fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
pytesseract==0.3.13
opencv-python-headless==4.10.0.84
numpy==2.2.1
python-multipart==0.0.20

# Respaldo de EasyOCR cuando Tesseract no consigue leer el nombre o el
# domicilio (ver `_leer_con_easyocr` en pipeline.py). APAGADO POR DEFECTO:
# hace falta poner DNI_OCR_EASYOCR=1 para activarlo.
#
# Probado en real: sobre una instancia de Render de 512 MB, cargar el modelo
# (~500 MB solo el motor) mataba el contenedor entero con SIGKILL (exit 137),
# no con una excepción que el código pudiera atrapar. Con menos de ~1 GB de
# RAM disponible para el servicio, no actives esta variable.
#
# El índice de PyTorch da la build solo-CPU: bastante más ligera que la que
# trae CUDA de serie, que aquí no hace ninguna falta.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
easyocr==1.7.2
